{
  "name": "resume-filter",
  "version": "1.0.0",
  "description": "A resume filter web app",
  "main": "server.js",
  "scripts": {
    "prestart": "cd client && npm run build",
    "start": "node server",
    "dev": "nodemon server",
    "postinstall": "cd client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Jason Kepler",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
